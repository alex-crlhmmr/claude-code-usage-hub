global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: "otel-collector"
    static_configs:
      # 'collector' is the docker-compose service name; reachable on the compose network.
      - targets: ["collector:8889"]
